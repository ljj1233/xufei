# 前端环境变量示例文件
# 复制本文件为.env并根据实际情况填写

# 应用运行模式，可选值：test（测试模式）、production（上线模式）
VITE_APP_MODE=test

# 后端API基础地址，例如：http://localhost:8000 或 https://api.example.com
VITE_API_URL=http://localhost:8000

# 其他可选环境变量可在此补充
# VITE_XXX=your_value

# 说明：
# - VITE_APP_MODE 控制前端权限与页面访问模式
# - VITE_API_URL 配置后端接口地址，需与后端服务保持一致